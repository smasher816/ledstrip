Setup

Make sure you have avr-gcc, avr-binutils, and avr-libc installed.

After cloning you must run `git submodule update --init`

Compiling

mkdir build
cd build
cmake ..
make
make upload

Scripts

flashall.sh - Flash a new chip to a working state
